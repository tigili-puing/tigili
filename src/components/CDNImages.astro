---
import { Image } from "astro:assets";

const {
    src,
    alt = "",
    width = 800,
    layout = "constrained",
    class: className = "",
    ...rest
} = Astro.props;
---

<Image
    src={src}
    alt={alt}
    width={width}
    layout={layout}
    class={className}
    inferSize={true}
    {...rest}
/>